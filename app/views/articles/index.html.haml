%h1{:style => "margin-top:100px;"}
  Все статьи
.row
  .col-md-8
    - @articles.each do |article|
      .row
        .row
          .col-md-12
            %iframe{ frameborder: 0, height: 300, src: article.video_link, width: "100%" } if article.video_link
        .row
          .col-md-12
            = truncate(article.body, length: 500, separator: ' ')
            = link_to("смотреть больше", article_path(article))
        .row
          .col-md-3
            = button_to "Редактировать статью", edit_article_path(article.id), method: :get
          .col-md-3
            = button_to "Удалить статью", article_path(article), method: :delete, data: { confirm: "Are you sure?" }
  .col-md-1
  .col-md-3
    .row
      - @hstory.each do |article|
        .row
          = link_to article_path(article.id) do
            .row
              .col-md-8
                = article.header
            .row
              .col-md-8
                = article.created_at.strftime("%b %e, %l:%M %p")
    .row
      .col-md-12
        = link_to "Все статьи", '#'
